import Navbar from '@/components/Navbar';
import ScrollScreen from '@/components/ScrollScreen';
import { Text } from '@/components/Text';

const categories = () => {
  return <ScrollScreen title="Categories"></ScrollScreen>;
};

export default categories;
